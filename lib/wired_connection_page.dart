import 'package:flutter/material.dart';

class WiredConnectionPage extends StatelessWidget {
  const WiredConnectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('wired connection'),
      ),
    );
  }
}
